[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hybrid-churn-project"
version = "0.0.1"

# --- ABSOLUTELY ESSENTIAL FOR SRC LAYOUT ---
[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

